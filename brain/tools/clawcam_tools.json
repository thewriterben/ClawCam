{
  "_deprecated": "This static tool catalog is no longer authoritative and is not read by any code. The single source of truth is the gateway MCP server: `gateway/clawcam_gateway/mcp_server/stdio_server.py` (TOOL_DEFINITIONS + APPROVAL_REQUIRED_TOOLS, exposed as `tool_catalog()`). The brain adapter discovers tools dynamically via MCP `tools/list`, and the gateway serves the same catalog at `GET /api/v1/tools`. Drift between these is guarded by `tests/gateway/test_tool_catalog_ssot.py`.",
  "version": "deprecated",
  "tools": []
}
