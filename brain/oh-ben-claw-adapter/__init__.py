"""Oh-Ben-Claw adapter for ClawCam gateway tools.

This directory is added to sys.path directly (the hyphenated name prevents
it from being imported as a package). Import the adapter module directly:

    from clawcam_adapter import ClawCamAdapter, ToolPolicy
"""
